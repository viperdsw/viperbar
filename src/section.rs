use gtk::prelude::*;
use gtk::{Box as GtkBox, Orientation, Window, WindowType};
use gtk_layer_shell::{Edge, Layer, LayerShell};

pub fn create_right_section(target_box: &GtkBox, module: Any) -> GtkBox {
    hbox.pack_end
}
